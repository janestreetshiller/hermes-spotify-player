# September 5, 2026 pre-push verification

Status: **automated checks and the live CUA checks below passed; remaining acceptance limits are explicit. This is branch submission evidence, not full release certification.**

Branch: `feat/low-resource-nokie-player`, existing PR #11. Local HEAD and the GitHub branch both resolved to `cf18cdab1feeec9c4304cb109efe6fef3826d8c7` during this run. Today's changes remain in the working tree.

## Checks executed in this run

- `HERMES_PYTHON=/Users/main/.hermes/hermes-agent/venv/bin/python npm test`: passed 19 JavaScript tests, 26 backend Python tests, 10 audio-manager Python tests, resource/vendor checks, isolated installer checks, and native DSP self-test using generated silence/tone input. The DSP self-test does not capture live audio.
- `npm run test:browser`: passed browser regression, 17 UI audits, audio fixtures, focus/motion checks, and ten reference layouts at 234/340/640px in full/off/mini states. Demo build passed.
- `npm run test:stress`: passed 144 geometry cases, all three finish control flows, mode cycling, and compact error recovery.
- `git diff --check`: passed.
- Browser tests refreshed 11 existing product PNGs in `docs/media/`, plus UI/audio/layout evidence. Desktop, spectrum, settings, and QuickTime fixture captures were visually inspected. These captures use simulated Spotify data; they are not live Hermes screenshots.

Raw logs are retained locally in `.test-output/qa-2026-09-05/` and excluded from Git.

## Live acceptance blocker

Three CUA initialization attempts failed: selecting Hermes timed out; surface discovery and app listing returned `Computer Use server error -10005: timed out waiting for initialize from codex app-server`. No live window state was obtained and no GUI regression actions were performed. The installed desktop and backend symlinks were inspected and point to this worktree, but this does not prove which code the running processes have loaded.

Full CUA acceptance remains pending: actual Hermes light/dark and narrow/default/expanded layouts; playback/track navigation/seek/volume with restoration; settings, styles, screen modes, keyboard focus, lyrics, audio enable/stop and lifecycle; authenticated search, liked-state and playlist flows with read-back and restoration. Current account authorization was not inspected. Earlier reports of OAuth being disconnected are historical, not current verification.

Restore the Codex Computer Use connection, rerun the live regression, review publishable screenshots, then commit the scoped changes and push this existing branch. Do not mark the live gate passed from these fixture results.

## CUA repair follow-up

The initialization blocker was cleared by terminating the stuck managed `SkyComputerUseService` helper and resetting the CUA REPL. A replacement service started, `cua.getState()` returned the app inventory, and `/Applications/Hermes.app` returned its live accessibility tree. Sidebar opening and player settings opening/closing were exercised successfully, with subsequent action/readback calls taking approximately 4–5 seconds. The initial Hermes selection was unusually slow. No permissions, credentials, or security settings were changed.

Full player regression remains pending. The inspected native settings did not expose the current reference-layout selector, including after invoking Reload desktop plugins; establish which plugin build is loaded before certifying it. Spotify was closed during the repair checks. No playback or account writes were performed, and nothing was committed or pushed in this follow-up.


## September 6 live CUA regression

The following checks used `/Applications/Hermes.app` through CUA, with real Spotify and the connected account. The new native Player style selector appeared after touching the source to trigger its file watcher. Rescan alone only discovers entries and did not refresh the already loaded module.

- Play and Pause in Hermes were independently confirmed by Spotify's Play/Pause control. Cold startup initially returned a confirmation timeout; retry after Spotify finished opening worked, and the player recovered.
- Search for a known song returned real results. Selecting its album result closed the panel and started the selected track. Next changed track metadata; Previous restarted the current track and then returned to the previous song.
- The current unliked song was liked, the UI read back Unlike/pressed state, then it was unliked and the original UI state returned. No lasting liked-library change was intended.
- Playlist rows loaded from the connected account, retained the selected track heading, and filtered by name. No playlist add/remove was performed.
- Synced lyrics loaded for the live track.
- Audio opt-in initially returned HTTP 405 because the pre-existing desktop backend lacked the new routes. Quitting/reopening Hermes loaded the routes, after which Enable entered SPOTIFY AUDIO and Stop returned to the off/consent view. No OS permission or security setting was changed.
- All ten reference layouts were selected through the native popup and their selected values read back, then Silver reference was restored. Chrome, Ice blue and Graphite were selected; mini, transport-only, full mode, and compact settings reopening were exercised. Artwork/full/chrome was restored. Existing browser tests provide geometric coverage; selection readback alone is not visual approval of every native layout.
- Pointer seek changed the position and native volume changes were exercised. AX slider value changes alone did not reliably commit to Spotify; do not treat an optimistic AX number as native readback. Final restoration used the real plugin's native controller: Chemistry paused at 165 seconds, requested volume 47 read back 46 due to Spotify quantization.
- `scripts/native-smoke.py` passed real pause/seek/volume checks and restoration. This is backend/native evidence separate from CUA. Source hash during this run: `77c81c1db3bfbb6e659acf989f92a930192aa20033cf8d92ff43af5b5ee7b284`.

### Remaining limits

No live playlist creation/add/remove, exhaustive native light/dark/size matrix, native Scope/Alchemy pixel progression, or sustained full-app CPU/RSS measurement was certified. Those checks remain release acceptance work. Audio/focus/error behavior and all layout sizes have the separately identified fixture coverage above. Public product shots are regenerated fixture images; private desktop/chat screenshots are not included.
